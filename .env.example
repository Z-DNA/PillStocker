# SUPABASE_KEY: cloud project = Publishable key (sb_publishable_...); local `supabase start` = anon key.
# Never use the Secret key (sb_secret_...) here — it bypasses Row-Level Security.
SUPABASE_URL=###
SUPABASE_KEY=###